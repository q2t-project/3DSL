---
import Layout from "../../layouts/Layout.astro";

// ※ ここは “実在パス” に合わせて調整してな。
// いまのプランでは /3dss/3dss/release/... を想定してる。
const links = [
  { label: "Latest schema", href: "/3dss/3dss/release/3DSS.schema.json" },
  { label: "v1.1.1 schema", href: "/3dss/3dss/release/v1.1.1/3DSS.schema.json" },
  { label: "v1.1.1 spec", href: "/3dss/3dss/release/v1.1.1/3DSS_spec.md" },
  { label: "Viewer docs", href: "/viewer/3DSD-viewer.md" },
];
---

<Layout title="Docs">
  <h1 class="text-2xl font-semibold">Docs</h1>
  <p class="mt-2 text-sm text-neutral-300">
    スキーマ・仕様・Viewer ドキュメントの入口。
  </p>

  <div class="mt-6 grid gap-3">
    {links.map((x) => (
      <a class="rounded-2xl border border-white/10 bg-white/5 p-4 hover:bg-white/10" href={x.href}>
        <div class="text-sm font-semibold">{x.label}</div>
        <div class="mt-1 font-mono text-xs text-neutral-400">{x.href}</div>
      </a>
    ))}
  </div>

  <div class="mt-10 rounded-2xl border border-white/10 bg-white/5 p-5 text-sm text-neutral-300">
    <div class="font-semibold text-neutral-100">方針メモ</div>
    <ul class="mt-2 list-disc space-y-1 pl-5">
      <li>schema_uri は $id 固定（バージョン別フラグメント付与せん）</li>
      <li>Viewer は構造層（.3dss.json）のみを入力とする</li>
      <li>表示設定の大半は Viewer 実装側（3DSS の範囲外）</li>
    </ul>
  </div>
</Layout>
