---
import Layout from "../../layouts/Layout.astro";
import ViewerDoc from "../../content/docs_integrated/3DSD-viewer.md";
---

const url = Astro.url;
const titleParam = url?.searchParams?.get("title") ?? null;

<Layout
  title="Viewer docs | Docs | 3DSL"
  description="3DSD-viewer.md を /docs 配下で表示します。"
>
  <div class="prose prose-invert prose-neutral max-w-4xl">
    <h1>Viewer docs</h1>
    <p>
      参照用の表示ページ。原文はサイト内部に統合して配信しています（/viewer 配下へ .md を直接公開しない設計）。
    </p>
    <ViewerDoc />
  </div>
</Layout>
